import { useEffect } from "react";

const Convites = ({ setTitulo, setActions }) => {

    useEffect(() => {
        setTitulo("Convites");
        setActions(null);
    })

    return (
        <>
        </>
    );
}

export default Convites;